# 写作功能精简记录

日期：2026-09-05。

## 保留范围

完整单元起草、逐章写作、作者约束、前情状态、候选审阅、改稿、证据问答、人工反馈、原稿与正式稿均保留。现有未提交的小说、分析器和研究修改没有重置。

## 移出范围

- `webnovel-writer/`：另一套历史写作系统，整体移出，不改造其内部代码。
- 根目录 `analyze_novel.py`、`analyze_qin_relationship.py`、`compare_approaches.py`、`EXPERIMENT_REPORT.md`：旧解析、特定人物分析与分析方法对比。
- `experiments/auxiliary_models_v1/` 中五个 `train_*.py`、本地模型基准、检查点验证、训练曲线、训练轮次调度及其依赖声明；同时移出四个仅测试这些已退役功能的测试文件。
- API 事实检查、人工标注及其数据工具仍在原处。`training_utils.py` 保留原名兼容旧导入，只保留 JSON 读写与分类评分，移除 PyTorch 导入和训练专用函数。文本/PDF 导入的可选依赖改在 `requirements-analysis.txt`。

历史报告和标注未删除。辅助实验旧 README 另存为 `reports/historical_training_workflow.md`，仅供历史阅读，不是当前可执行操作指南。

## 保护与恢复

操作前完整快照位于本机用户目录 `Documents/Claudenovel-archives/20260905-225734-writing-cleanup/before-cleanup.zip`，排除 `.git`、Python 和 pytest 缓存，共 3239 个文件，逐项 SHA-256 校验通过。此本地快照含用户项目和环境配置，供本机恢复，不随仓库或插件分发。

同目录的 `removed/` 保留移出项的原相对路径，`removed-targets.json` 为 24 个目标清单。`sha256.json`、`git-status.txt` 与 `git-head.txt` 记录操作前证据。需要恢复时只取所需文件到独立目录比对，避免整包覆盖后续改动。未改写 Git 历史，未提交或推送。

## 插件变化与验证方式

插件增加完整单元写作技能和完整运行代码，保留分析、报告与单章改写技能。以根目录源码为准，`scripts/sync_plugin.py` 同步，`--check` 只读检查发行副本。插件不打包小说、密钥、实验或生成结果。

分析入口不再把 `requirements.txt` 当默认小说；插件中没有原文时要求明确输入。检索基准中的共享检索实现、包初始化依赖和逐章评测模块保持可用。

验证包括保留测试集、插件清单/技能校验、独立目录里的入口与中文分析、单元生成和恢复测试。真实供应商文学质量需要另外的作者读稿验证，本轮不调用付费模型来评价文学质量。

实际结果：保留测试集 302 项通过，新增插件测试 8 项通过；仓库外独立插件执行完整单元运行器测试 30 项通过（模拟模型响应，覆盖生成、恢复、修订和正式稿保护）。四个技能及插件清单校验通过，57 个发行源文件与主源码一致。API 事实检查在禁止导入 torch/transformers 的进程中可导入并完成评分。

最终哈希核对：456 个移出文件与归档逐字节一致，2761 个原有文件未变；无预期外修改或丢失。本轮修改范围的 `git diff --check` 通过；全工作树检查仍会报告用户原有小说文件中的行尾空白，本轮保留其原字节。

本轮更新的是仓库插件源码和发行副本，没有重新安装 Codex/Claude 的已有插件缓存。未宣称当前已打开的任务已加载新技能。
